<section class="splash">
    <div class="container text-center">
        <div class="row">
            <div class="col-sm-4">
                <h1 class="intro-title"><%= @rate.origin.port_code %></h1>
                <h2><%= @rate.origin.name %></h2>
            </div>
            <div class="col-sm-4 ">
                <h1>
                    <i class="glyphicon glyphicon-arrow-right intro-title"></i>
                </h1>
            </div>
            <div class="col-sm-4">
                <h1 class="intro-title"><%= @rate.destination.port_code %></h1>
                <h2><%= @rate.destination.name %></h2>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-sm-6">
              <h1>Seller</h1>
                <h2 style="color: rgb(51, 122, 183)"><%= @rate.seller.name %></h2>
            </div>
            <div class="col-sm-6">
              <h1>Carrier</h1>
                <h2 style="color: rgb(51, 122, 183)"><%= @rate.carrier.name %></h2>
            </div>
            <div class="col-sm-4">
                <h1>Mode</h1>
                <h2 style="color: rgb(51, 122, 183)"><%= @rate.shipment_category.shipment_type.name %></h2>
            </div>
            <div class="col-sm-4">
                <h1>Category</h1>
                <h2 style="color: rgb(51, 122, 183)"><%= @rate.shipment_category.name %></h2>
            </div>
            <div class="col-sm-4">
                <h1>Commodity</h1>
                <h2 style="color: rgb(51, 122, 183)"><%= @rate.commodity.name %></h2>
            </div>
            <div class="col-sm-6">
              <h1 >Cost</h1>
                <h2 style="color: rgb(51, 122, 183)"><%= number_to_currency(@rate.cost) %></h2>
            </div>
            <div class="col-sm-6">
              <h1>U.O.M.</h1>
              <h2 style="color: rgb(51, 122, 183)"><%= @rate.unit_of_measure.name %></h2>
            </div>
        </div>
        <br>
        <% if current_user.admin? %>
        <%= link_to 'Edit', edit_rate_path(@rate), class: "btn btn-lg btn-warning" %>
        <% end %>
        <%= link_to 'Back', rates_path, class: "btn btn-lg btn-info" %>
    </div>
</section>
